import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  button: {
    borderRadius: 10,
    backgroundColor: 'blue',
    alignItems: 'center',
    justifyContent: 'center',
    height: 50,
    width: 200
  },
  title: {
    color: 'white',
    fontSize: 20
  }
});

export default styles;