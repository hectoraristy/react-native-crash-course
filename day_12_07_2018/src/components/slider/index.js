import React, { Component } from 'react';
import {
  Text,
  Slider
} from 'react-native';

const SliderCustom = (props) => {
    return (
      <Slider {...props} />
    );
}

export default SliderCustom;

